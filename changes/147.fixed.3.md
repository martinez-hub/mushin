`dry_run` reports the sampled cell count when `sample=` is set (the cell-count gate still applies to the full grid, since every cell is launched regardless of sampling); a fresh run reusing a `working_dir` no longer inherits the prior sweep's `notes`/`tags`, and `tags=[]` on resume clears them.

`dry_run` reports the sampled cell count and `confirm_above` gates on what will actually run when `sample=` is set; a fresh run reusing a `working_dir` no longer inherits the prior sweep's `notes`/`tags`, and `tags=[]` on resume clears them.

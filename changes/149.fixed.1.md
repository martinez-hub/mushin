`HydraDDP`'s docstring example now runs on supported Lightning (`devices=2`, not the removed `gpus=2`); the multinode example uses the standard workflow API; the multinode guide gained a "Preemption & resume" section covering SLURM requeue and recovery.

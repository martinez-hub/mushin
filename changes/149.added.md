`run()` accepts `launcher_config=` — launcher fields (e.g. from `submitit_slurm_config()`) are applied as `hydra.launcher.*` overrides internally, so cluster sweeps no longer need hand-rolled override strings.

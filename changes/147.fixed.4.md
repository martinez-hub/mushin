`show`/`export.table`: a metric sharing a swept param's name renders as `<name> (metric)` instead of overwriting the param column, and a typo'd `metrics=` entry raises instead of silently dropping the column.

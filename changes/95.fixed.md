`HydraDDP`/`HydraFSDP` docs now show the working launcher-provided-ranks pattern; an imperative `@mushin.sweep` task with `strategy=HydraDDP(), devices=N` on the local launcher silently trains on one GPU.

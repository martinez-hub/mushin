Capped the `mcp` extra's dependency to `mcp >= 1.2, < 2`: the MCP Python SDK
2.0 removes the `mcp.server.fastmcp` module that mushin's MCP server is built
on, so an uncapped install of `mushin-py[mcp]` would break at import time.

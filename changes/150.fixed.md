Provenance now records the Apple Silicon MPS accelerator (`device: "mps (<chip>)"`) when CUDA is absent — M-series runs are no longer hardware-blind; the install guide documents Apple Silicon support explicitly.

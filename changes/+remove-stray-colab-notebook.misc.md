Removed `colab-notebook/mushin_llm_examples.ipynb`, a scratch notebook that Colab's "Save a copy in GitHub" pushed directly to `main`. It was a throwaway used to review the LLM examples in a clean environment, it carried all of its execution outputs (pre-commit's `nbstripout` never ran on it), and one of its ad-hoc cells failed `ruff check`, which broke lint on every open pull request.

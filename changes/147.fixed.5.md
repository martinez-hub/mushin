`compare_methods(allow_incomplete=True)` masks constant (zero-variance) methods over the completed seeds and keeps ±Inf cells in the pairing instead of silently excluding them; `summary(reference=...)` rejects unknown method names.

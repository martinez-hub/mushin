Fail-soft, sampled, and budget-skipped sweeps can now be reloaded from disk: failed/skipped cells NaN-fill instead of `load_metrics` raising `FileNotFoundError`, and a stale sidecar in a failed cell's dir is never loaded.

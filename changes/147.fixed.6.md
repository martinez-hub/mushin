MCP `get_failures`/`get_provenance` raise for a nonexistent path instead of reporting an empty-but-valid result; `MetricsCallback` writes standalone `trainer.validate()` metrics to `validate_metrics.pt`; `seed_everything_per_rank` falls back to `LOCAL_RANK`; provenance records `dirty: null` when `git status` cannot be verified.
